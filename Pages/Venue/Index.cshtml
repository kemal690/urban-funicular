@page
@model Database_and_web_assignment.Pages.Venue.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Venues[0].Venue_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Venues[0].Venue_Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Venues) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Venue_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Venue_Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Venue_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Venue_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Venue_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
