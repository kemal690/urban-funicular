@page
@model Database_and_web_assignment.Pages.Teacher.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Teachers[0].Teacher_First_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teachers[0].Teacher_Last_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teachers[0].Teacher_Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teachers[0].Teacher_Mobile_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teachers[0].Teacher_Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Teachers) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher_First_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher_Last_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher_Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher_Mobile_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher_Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Teacher_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Teacher_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Teacher_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
