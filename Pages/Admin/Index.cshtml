@page
@model Database_and_web_assignment.Pages.Admin.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Admins[0].Admin_First_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admins[0].Admin_Last_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admins[0].Admin_Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admins[0].Admin_Mobile_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admins[0].Admin_Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Admins) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_First_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_Last_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_Mobile_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin_Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Admin_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Admin_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Admin_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
